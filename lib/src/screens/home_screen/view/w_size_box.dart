import 'package:flutter/material.dart';
class WSizeBox extends StatelessWidget {
  const WSizeBox({super.key});

  @override
  Widget build(BuildContext context) {
    return  Expanded(
      child: SizedBox(),
    );
  }
}
